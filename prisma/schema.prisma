generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Cocktail {
  id           Int     @id @default(autoincrement())
  name         String
  category     String
  instructions String
  image        String?
}
